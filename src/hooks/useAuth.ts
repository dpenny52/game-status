/**
 * useAuth Hook
 *
 * Re-exports the useAuth hook from AuthContext for convenience.
 *
 * @module useAuth
 */
export { useAuth } from "../context/AuthContext";
export type { AuthContextType, User, AuthState } from "../context/AuthContext";
